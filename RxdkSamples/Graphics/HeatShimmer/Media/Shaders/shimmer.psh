xps.1.1

tex t0;                 // sample distortion.x, distortion.y, 1
texm3x2pad t1, t0_bx2;  // u0 = distortion.x*scale + screenspace.x
texm3x2tex t2, t0_bx2;  // v0 = distortion.y*scale + screenspace.y, sample color texture

mov r0, t2              // show distorted color value
