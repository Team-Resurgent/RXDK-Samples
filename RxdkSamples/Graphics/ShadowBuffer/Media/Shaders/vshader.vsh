;------------------------------------------------------------------------------
; Vertex shader for the shadow buffer
;------------------------------------------------------------------------------
xvs.1.0

;------------------------------------------------------------------------------
; Constants specified by the app
;    c0-c3 = WVP Matrix (WORLD*VIEW*PROJECTION)
;    c4-c7 = WT Matrix (WORLD*TEXTURETRANSFORM)
;    c8    = Local space light position.
;
; Vertex components (as specified in the vertex DECL)
;    v0    = Position
;    v1    = Normal
;    v2    = TexCoord0
;------------------------------------------------------------------------------

;------------------------------------------------------------------------------
; Vertex->screen
;------------------------------------------------------------------------------
dp4 oPos.x, v0, c0
dp4 oPos.y, v0, c1
dp4 oPos.z, v0, c2
dp4 oPos.w, v0, c3

;------------------------------------------------------------------------------
; Diffuse lighting
;------------------------------------------------------------------------------
add r0, c8,-v0
dp3 r0.w, r0, r0
rsq r1.x, r0.w
mul r0, r0, r1.x
dp3 oD0, v1, r0

;------------------------------------------------------------------------------
; Base texture
;------------------------------------------------------------------------------
mov oT0, v2

;------------------------------------------------------------------------------
; Vertex->shadowbuffer texcoords
;------------------------------------------------------------------------------
dp4 oT1.x, v0, c4
dp4 oT1.y, v0, c5
dp4 oT1.z, v0, c6
dp4 r0.w, v0, c7

; Clamp w (q) to 0
slt r1, c0, c0
max r0.w, r0.w, r1.w
mov oT1.w, r0.w
